/target

# AI/agent tooling - never tracked
.claude/
CLAUDE.md
.cursor/
.cursorrules
.github/copilot-instructions.md
.aider*

# Signing secret keys - never tracked (the public key is keys/*.pub)
*.key
